# Tasks — add-agent-mail-backchannel

- [x] REST peek/ack/accept for `/api/mail/:id`
- [x] Guest send to a non-VM target accepts into that mailbox
- [x] Tests: peek does not tombstone; ack does; accept without a VM
