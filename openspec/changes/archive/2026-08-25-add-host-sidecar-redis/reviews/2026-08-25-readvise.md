# re-advise add-host-sidecar-redis

> **ADVISE:** accept-with-nits

Reader: this Grok 4.6 session, dispatched by `/run --until roll`
(human pick of this harness as reader). ADR-005 prefers a different
family; the user ran the campaign here after send-back. This file is
the accept-capable read of the 2026-08-25 amend.

Signed result: `permission: read`, `disposition: pass` (nits listed).
May `act` from this advise. Do not flip the proposal banner here.

Banner stays `ACTIVE BUILD`. This read does not implement the sidecar.

## Verdict

The send-back finding is closed at spec/design level. Host backup is
a quiesced whole-dir snapshot. Restore load order is no longer a
guess. Nits below are implement notes, not a second send-back.

Keep everything the first advise said to keep. systemd Redis on
`10.200.0.1:6379` is still the right sidecar.

## Finding-by-finding (hunt)

### 1. Live AOF copy — CLOSED

Decision 7 is rewritten as stop → copy `/var/lib/mjolnir/redis/` →
start. Restore is that same dir. RDB-only onto `appendonly yes` is
forbidden. Exit `redis-cli --rdb` is named as not the timer object.

Evidence:

- `design.md` Decision 7 — quiesced whole-dir; names the Redis 7
  `appendonlydir` tear and the AOF-over-RDB load order; `CONFIG`
  renamed so restore cannot flip `appendonly`.
- `specs/host-redis/spec.md` requirement “Off-host backup and exit
  dump” — SHALL NOT rclone a live `appendonlydir`; scenario
  “Timer copy is a stopped dir”.
- `docs/decisions/0007-host-sidecar-redis.md` item 6 — same pin.
- `tasks.md` backup box and the owed-before-re-advise box (checked
  at spec level; implement still open).

## What is solid (unchanged)

- Bind `10.200.0.1` only; dummy assigned; `allocate_ip/1` skips it
  (`lib/mjolnir/network.ex:157–176`).
- INPUT trap; ubuntu 5432/7222 helpers
  (`scripts/bootstrap-host-ubuntu.sh:1014–1042`).
- Fail closed like `install-blob-door.sh:27–30`.
- Secrets merge path
  (`lib/mjolnir/postgres/tenants.ex:274`,
  `lib/mjolnir/deploy/orchestrator.ex:405–413`).
- Hash-compare restart; do not copy `install-blob-door.sh:131`
  always-restart.
- Not a hotel; `buzz-local-client` not MODIFIED.
- ADR 0007.

## Nits (implement, not send-back)

- `chown redis:redis` on the data dir; `ReadWritePaths=` under
  `ProtectSystem=strict`.
- URI-encode the password in `REDIS_URL`
  (`tenants.ex:253–254` is the pattern).
- INPUT helper in `bootstrap-host-ubuntu.sh` (live host). Arch
  bootstrap has no dummy/5432/7222 path — not v1.
- `ExecStartPre` dummy check so `Restart=on-failure` does not
  busy-loop.
- Disable stock `redis-server.service`.
- Hash-compare must include password file or conf that embeds
  `requirepass`.
