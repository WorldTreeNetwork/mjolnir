# steer add-honor-being

**When.** 2026-09-10
**Depth.** standard
**Bead.** `mjolnir-x97p.1` (epic `mjolnir-x97p`)

Activate `.1` opened this pass. Host picker declined; remaining HIGH
forks took the recommended column (decide-for-me). MEDIUM/LOW
auto-logged, not menued.

## Decided

- Whose identikey: friend C2 managed identity; the VM is a **device**
  of that XID; Duke may hold Elect recovery (decide-for-me / activate)
- Grok login split: IdentiKey **passkey gates the being** (browser
  `/term` + preview). Grok inference is `XAI_API_KEY` or one grok.com
  device-auth stored in the VM. Do not set `GROK_OIDC_ISSUER` to
  identikey unless an xAI chat proxy exists (decide-for-me)
- VM git key: Elect-gated **device/credential row + injected SSH
  key**; `git config gpg.format ssh`. Not a second XID Sign key
  (decide-for-me)
- Git host: Forgejo on mimir only for push
  (`VirtueInnova/hypersigil-store-frontend`). GitHub origin is not
  the deploy trigger (auto)
- Preview URL: Iroh ticket `https://<ticket>.vm.worldtree.network`
  for v1. Named subdomain later (auto)

## Skipped

None.

## Feeds change

A hosted being is a long-lived ubuntu-24.04 VM (snapshot of a bootstrap, not
a new `@base` name) for a non-technical vibe coder. The friend's
C2 identikey is the identity; the VM is a device of it. Browser
passkey at `auth.identikey.me` opens wrug `/term` (tmux `session=main`)
and the Vite preview. grok in the guest is an xAI client. Git
commits are SSH-signed with a SecretStore-opaque key injected over
vsock; the pubkey is an identikey device and a Forgejo write key.
Frontend talks to `https://api.hypersigil.world`. Push to Forgejo
`main` is the prod cutover. Code is later landings after advise
accept: `add-identikey-being-client`, `add-vm-git-subkey`,
`add-honor-git-remote`, `add-honor-dev-preview`,
`update-hypersigil-store-cors`.

Send-back amend 2026-09-10 (Fable): A3 + consent-log not Elect;
kind `ssh_git`; `owner_id` = public XID `sub`; respawn is a new
device; `XAI_API_KEY` only in tmpfs; passkey gates `/term` not
the preview; per-ticket CORS; `git push main` is accepted
prod-cutover risk.
