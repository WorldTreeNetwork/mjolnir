# Tasks

- [x] Blake3 + base58 helper matching the door (`blob/b3/{b58}`)
- [x] Door client: PUT/GET/HEAD, no B2 credentials
- [x] SQLite `blob` row: hash, size, content-type. No bytes column
- [x] `POST /blob` and `GET /blob/{hash}`
- [x] Tests: hash mismatch never stored; put then get; fail closed without door URL
